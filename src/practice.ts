let count = 0;
count += 1;
// count = 'Sudden String Came here!';

const message:string = 'hello world';

const done:boolean = true;

const numbers: number[] = [1, 2, 3]; // Array of Numbers.
const messages: string[] = ['hello', 'world']; // string array.

// message.push(1); // not available.

let mightBeUndefined: string | undefined = undefined; // string or undefined.
let nullableNumber : number | null = null; // number or null.

let color: 'red' | 'orange' | 'yellow' = 'red'; // only can be set with 'red', 'orange', 'yellow'

color = 'yellow';
// color = 'green'; //not available.