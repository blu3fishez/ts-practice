"use strict";
var count = 0;
count += 1;
// count = 'Sudden String Came here!';
var message = 'hello world';
var done = true;
var numbers = [1, 2, 3]; // Array of Numbers.
var messages = ['hello', 'world']; // string array.
// message.push(1); // not available.
var mightBeUndefined = undefined; // string or undefined.
var nullableNumber = null; // number or null.
var color = 'red'; // only can be set with 'red', 'orange', 'yellow'
color = 'yellow';
// color = 'green'; //not available.
